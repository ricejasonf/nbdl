#ifndef NBDL_VALIDATE_HPP
#define NBDL_VALIDATE_HPP

#include<boost/optional.hpp>

namespace nbdl {

template<class ErrorBinder, class Entity>
void validate(ErrorBinder &errors, Entity &entity)
{
	
}

template<class ErrorBinder, class M>
void validateMember(ErrorBinder &errors, M member

}//nbdl
#endif
